const frutas = ['Maca', 'Uva', 'Limao'];

for(let i in frutas){
    console.log(frutas[i]);
}

const pessoa = {
    nome: "Vinicius",
    sobrenome:"Kruger",
    idade: 18,
};

for(let i in pessoa){
    console.log(pessoa[i]);
}